class MixViewModel {
  int id;
  String title;
  String description;
  int minutes;

  MixViewModel(
      {required this.id,
      required this.title,
      required this.description,
      required this.minutes,
      s});
}
